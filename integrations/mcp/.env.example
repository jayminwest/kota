# Example .env file for MCP client secrets
# Copy this to your mcp-ts client directory (e.g., path/to/mcp-client/.env)
# Add your actual API keys and tokens below.
# Make sure the actual .env file is listed in your .gitignore!

# Example for a hypothetical weather server
# WEATHER_API_KEY=your_api_key_here

# Example for Google Workspace (replace with real values)
# GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET
# GOOGLE_REFRESH_TOKEN=YOUR_GOOGLE_REFRESH_TOKEN

# Example for GitHub (replace with real values)
# GITHUB_TOKEN=your_github_personal_access_token
